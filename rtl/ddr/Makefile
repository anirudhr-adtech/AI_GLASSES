# Makefile for DDR subsystem verification
SUBSYSTEM := ddr
RTL_DIR   := $(CURDIR)
TB_DIR    := $(CURDIR)/tb
OBJ_DIR   := $(CURDIR)/obj_dir

# Module search paths for this subsystem
YDIRS     := -y $(RTL_DIR)/ -y $(RTL_DIR)/../../sim/common/

include ../../sim/common/Makefile.inc
